[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dopython"
version = "0.1.0"
description = "Wandrille Buchy's personnal functions"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "seaborn",
    "matplotlib"
    "pandas",
]